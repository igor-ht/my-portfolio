import { Indie_Flower, Roboto_Mono } from 'next/font/google';

export const indie_flower = Indie_Flower({ subsets: ['latin'], weight: '400', display: 'swap' });
export const roboto_mono = Roboto_Mono({ subsets: ['latin'], weight: '400', display: 'swap' });
